[tool.black]
line-length = 88
target-version = ['py310']

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP"]

[tool.ruff.lint.isort]
known-third-party = ["fastapi", "torch", "pandas", "numpy"]

[tool.pyright]
include = ["src"]
extraPaths = ["."]